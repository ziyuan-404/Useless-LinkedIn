interface:
  display_name: "Useless LinkedIn"
  short_description: "经历库、A–H 岗位评估、定制材料、投递与漏斗复盘"
  default_prompt: "Use $useless-linkedin to check this job, run Knock-out and A–H evaluation, then route it to the appropriate application workflow."
policy:
  allow_implicit_invocation: true

# 建档

1. 读取 `modules/personal-career-os/MODULE.md`，从用户提供的母版和确认中建立 `.useless-linkedin/profile/` 唯一事实库；保留原件与来源日期。
2. 确认 `.useless-linkedin/operations/` 中的筛选、简历、回答与跟进规则。未知身份、资格、薪资等保持待确认。
3. 检查海投简历审计与真实 Dashboard 是否可用。授权范围按 `policies/authorization.md` 记录，不由模板推断。
4. 完成一次只找岗的试运行，再根据用户要求进入后续阶段。
